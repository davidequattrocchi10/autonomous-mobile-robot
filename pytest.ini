[pytest]
# Only look for tests inside the tests/ directory.
# Without this, pytest's default recursive search picks up files like
# examples/test_gridworld.py (which match test_*.py) and runs their
# module-level code as a side effect — triggering matplotlib windows etc.
testpaths = tests
